# secrets / env — stay local, never commit
.env
.env.*
!.env.example

# deps / build
node_modules/
dist/
build/
.next/
.vercel/

# os / editor
.DS_Store
*.log

# agent working files — kept on disk, out of the repo's mental model (calm root).
# Flip these back on if you want them tracked for cross-device backup.
.claude/
memory/
brainstorms/

# private fixtures (real/redacted data the public tree shouldn't carry)
fixtures/private/
