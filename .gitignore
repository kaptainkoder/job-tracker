# secrets / env — stay local, never commit
.env
.env.*
!.env.example

# deps / build
node_modules/
dist/
build/
.next/
.vercel/

# os / editor
.DS_Store
*.log

# agent working files — kept on disk, out of the repo's mental model (calm root).
# Anything that exists for Claude/Codex (plans, memory, agent instructions, verification
# hand-offs) stays local. Flip these back on if you want them tracked for cross-device backup.
.claude/
.Codex/
.agents/
AGENTS.md
CLAUDE.md
START_HERE.md
KICKOFF_BRIEF.md
memory/
brainstorms/
design/
docs/codex-tests/

# private fixtures (real/redacted data the public tree shouldn't carry)
fixtures/private/
.vercel
