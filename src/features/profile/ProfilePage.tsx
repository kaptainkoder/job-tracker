// Placeholder — the profile store (form + resume upload to Supabase storage) is chunk A3.
export default function ProfilePage() {
  return (
    <div className="animate-rise space-y-4">
      <h1 className="text-2xl font-semibold text-ink">Profile</h1>
      <div className="card max-w-md p-4 text-sm text-ink-soft">
        Personal info, current job, LinkedIn, GitHub, and your base resume live here. Built in
        chunk A3.
      </div>
    </div>
  );
}
